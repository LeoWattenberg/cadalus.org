export default {
  defaultLocale: "en",
  locales: ["en", "fr"],
  // Use only the 'features' namespace (remove unused 'common' to avoid missing file lookups)
  namespaces: ["features"],
  defaultNamespace: "features",
};