---
import RootPage from '../index.astro';
import { supportedLocales } from '../../i18n/config';

export function getStaticPaths() {
	return supportedLocales.map((lang) => ({ params: { lang } }));
}
---
<RootPage />
