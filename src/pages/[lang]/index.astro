---
import RootPage from '../index.astro';
import { parseLocaleFromPath } from '../../i18n/astro';
const locale = parseLocaleFromPath(Astro.url.pathname);
// Optionally could validate locale here.
---
<RootPage />
